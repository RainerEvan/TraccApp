type Applications{
    id:ID!
    name:String
}

extend type Query{
    getAllApplications:[Applications]
    getApplication(applicationId:ID!):Applications
}

extend type Mutation{
    addApplication(name:String!):Applications
    deleteApplication(applicationId:ID!):Boolean
}