type Comments{
    id:ID
    ticket:Tickets
    author:Accounts
    createdAt:DateTime
    updatedAt:DateTime
    body:String
    isActive:Boolean
}

extend type Query{
    getAllCommentsForTicket(ticketId:ID!):[Comments]
}

extend type Mutation{
    addComment(
        ticketId:ID!,
        body:String!
    ):Comments

    editComment(
        commentId:ID!,
        body:String!
    ):Comments

}