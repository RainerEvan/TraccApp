type SupportAttachments{
    id:ID
    support:Supports
    fileBase64:String
    fileName:String
    fileType:String
}

extend type Query{
    # getSupportAttachment(attachmentId:ID!):SupportAttachments
    # getAllSupportAttachmentsForSupport(supportId:ID!):[SupportAttachments]
}