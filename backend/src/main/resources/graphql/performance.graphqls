type Performance{
    menu:String
    period:String
    totalPending:Int
    totalInProgress:Int
    totalResolved:Int
    totalDropped:Int
    totalTickets:Int
    rate:String
    label:[String]
    data:[Int]
}

extend type Query{
    getPerformanceForDeveloper(accountId:ID!):[Performance]
}