type Supports{
    id:ID
    ticket:Tickets
    dateTaken:Date
    developer:Accounts
    result:String
    description:String
    tags:[Tags]
    devNote:String
    isActive:Boolean
    attachments:[SupportAttachments]
}

extend type Query{
    getAllSupportsForDeveloper(accountId:ID!):[Supports]
}

extend type Mutation{
    addSupport(
        ticketId:ID!
    ):Supports

    solveSupport(
        supportId:ID!,
        result:String!,
        description:String!,
        devNote:String!,
        tags:[String]!
    ):Supports

    withdrawSupport(
        supportId:ID!,
        result:String!,
        description:String!,
    ):Supports
}